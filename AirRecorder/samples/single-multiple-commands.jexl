##
## Distinguish between single/multiple result(s)
##

if (airrecorder.resultlist) {
  ## Just executed a command with variable expansion
  airrecorder.stderr.println(size(airrecorder.results));
  
  ## Print executed commands
  for (result : airrecorder.results) {
    airrecorder.stderr.println(result.query.command);
  }

} else {
  ## Single command with no variables
  airrecorder.stderr.println(airrecorder.query.command);  

}

return false;
